# 项目设计计划书

## 1. 项目目标

本项目是一个面向 Telegram 群聊的 AI 机器人，后续可扩展到 Discord、Matrix 等平台。机器人不是固定规则聊天程序，而是一个 AI agent：

- 能理解文本、图片、表情包/贴纸等消息。
- 能在需要时调用工具，由模型决定何时调用、调用哪个工具。
- 能统计使用量，并在超量时自动切断。
- 能限制工具权限，避免模型越权访问服务器资源。
- 能允许用户自带 API Token，切换到自己的 LLM provider。
- 能保留未来扩展能力，但当前优先完成 Telegram 场景。

## 2. 核心设计原则

1. 工具由模型选择，不由路由器预分流。
2. 宿主程序负责授权、执行、审计和限额控制。
3. 模型不能直接接触服务器文件系统、shell 或其他高风险能力。
4. 所有能力都应通过明确的接口暴露，默认拒绝，按需放行。
5. 平台适配层与 AI 核心层解耦，避免绑定 Telegram 语义。
6. 交互体验以“先占位、后编辑”为默认方式，减少群聊刷屏。

## 3. 总体架构

系统分为四层：

1. 平台适配层
   - 负责接收消息、发送消息、编辑消息、处理附件。
   - 当前实现 Telegram，未来可补 Discord、Matrix。

2. AI 核心层
   - 负责组织上下文、调用 LLM、处理 tool call、整合回复。
   - 采用 `rig` 作为 agent/runtime 层。

3. 工具与权限层
   - 负责计算器、联网检索、未来的文件工具等。
   - 负责权限校验、额度校验、审计记录。

4. 数据与配置层
   - 负责会话、消息、用户设置、provider 配置、用量账本、授权记录。
   - 建议使用 SQLite 起步，后续可迁移到 PostgreSQL。

## 4. 消息生命周期

默认交互流程如下：

1. 收到用户消息后，先发送一条占位回复，例如“正在处理...”。
2. 将占位消息的 `message_id` 记录到本次会话上下文中。
3. 将用户输入交给 agent。
4. LLM 如需工具，则发起 tool call。
5. 宿主在执行前做权限与额度检查。
6. 工具返回结果后，继续回传给 LLM。
7. 最终答案通过编辑占位消息完成。

编辑策略：

- 工具执行阶段可把占位文字更新为“正在计算...”“正在查询资料...”。
- 流式输出时，按节流频率编辑同一条消息，避免频繁刷 API。
- 如果消息不可编辑或编辑失败，则降级为发送新消息。

## 5. 工具体系

### 5.1 工具调用模型

工具不是路由器，不负责预判用户意图后分流。工具是 agent 的能力集合，由 LLM 在对话中自行决定是否调用。

### 5.2 第一阶段工具

1. 工具目录与接口先保留。
   - 先定义 schema、风险等级、权限检查入口和审计入口。
   - 暂不落地计算器、联网检索等具体执行逻辑。

2. 具体工具后置。
   - 等 Telegram 消息流、agent/runtime 和权限/额度骨架稳定后再逐个实现。

### 5.3 后续工具

- 文件读取
- 文件写入
- 日志查询
- 数据库查询
- 其他业务工具

这些工具都必须经过权限和沙箱设计后才能开放。

## 6. 权限与隔离

### 6.1 权限模型

- 默认拒绝。
- 每个工具必须定义能力范围、风险等级和是否需要确认。
- 工具调用前由宿主检查用户、群组、会话和工具本身的策略。

### 6.2 高风险边界

以下能力不应直接暴露给模型：

- 任意文件系统访问
- 任意 shell 执行
- 任意网络写操作
- 整个宿主机环境变量读取

如果未来需要这些能力，必须放入独立 worker，并使用最小权限、白名单路径、超时和审计机制。

### 6.3 审计要求

- 所有工具调用必须记录。
- 记录内容至少包括：用户、群组、工具名、参数摘要、执行结果、耗时、是否消耗预算。

## 7. 使用量统计与超量切断

### 7.1 统计目标

系统需要统计：

- 模型 token 用量
- 工具调用次数
- 工具调用耗时
- 可能的外部 API 成本

### 7.2 切断策略

1. 请求前预检剩余额度。
2. 请求中尽量控制 `max_tokens`。
3. 请求后把 usage 写入账本。
4. 超量时直接拒绝继续调用。
5. provider 不返回 usage 时，用估算值兜底并标记为估算。

### 7.3 配额维度

建议至少支持：

- 按用户
- 按群组
- 按会话
- 按 provider

## 8. Provider 切换与 BYOK

### 8.1 目标

允许用户接入自己的 API Token，使用自己的 LLM provider 生成回复。

### 8.2 推荐方式

- 不建议把 token 当作普通聊天内容直接收集。
- 建议通过私聊里的 `/connect` 或 `/provider` 流程进入配置。
- 用户在安全页面或 Telegram Web App 中填写 provider、base_url、model、api_key。
- 服务端加密保存。
- 运行时按用户配置、群配置、全局默认的优先级选择 provider。

### 8.3 安全要求

- token 不应进入普通日志。
- token 不应在错误报告中泄露。
- token 应支持撤销、更新和测试连接。

### 8.4 provider 目标

当前优先：

- 本机 Ollama
- OpenAI-compatible provider

后续可扩展：

- OpenAI
- Anthropic
- Gemini
- 其他兼容接口

## 9. 多平台预留

虽然当前只做 Telegram，但架构上要提前保留多平台能力：

- Telegram
- Discord
- Matrix

统一抽象应包含：

- 消息收发
- 消息编辑
- 附件处理
- 线程/回复关系
- 平台能力矩阵
- 身份映射

平台差异必须收敛到适配层，核心 AI 和权限逻辑保持平台无关。

## 10. 建议的技术栈

基础建议：

- `tokio`
- `serde`
- `serde_json`
- `tracing`
- `tracing-subscriber`
- `dotenvy`
- `anyhow`
- `thiserror`
- `sqlx`
- `teloxide`
- `rig-core`

可选增强：

- `reqwest`
- `uuid`
- `chrono`
- `rust_decimal`
- `dashmap`

## 11. 推荐实施顺序

### 阶段 1：MVP

- Telegram 实时收发入口。
- 占位消息 + 编辑回复。
- agent/runtime 基础链路，默认接本机 Ollama DeepSeek。
- 工具接口占位，不实现具体工具。
- 用量统计与超量切断。

### 阶段 2：安全增强

- 权限策略引擎。
- 审计日志。
- BYOK 配置流程。
- provider 动态切换。

### 阶段 3：扩展能力

- 文件工具沙箱。
- 更多业务工具。
- Discord / Matrix 适配层。
- 更完整的会话记忆与知识检索。

## 12. 设计约束总结

1. 模型可以决定是否调用工具。
2. 宿主必须决定工具是否允许执行。
3. 模型不能直接碰宿主机敏感资源。
4. 交互默认先占位，再编辑。
5. 能力默认关闭，按需开放。
6. 平台可切换，核心不绑定某个平台。
7. BYOK 只改变模型来源，不改变宿主安全边界。

## 13. 目录与模块约定

项目统一采用 `mod_name.rs` + `mod_name/mod_name.rs` 的布局，不使用 `mod.rs`。

- 顶层模块放在 `src/<name>.rs`。
- 对应实现放在 `src/<name>/<name>.rs`。
- 如果继续拆分子模块，也沿用同样的同名目录 + 同名文件方式递归展开。
- `lib.rs` 和 `main.rs` 作为入口文件保留，不使用 `mod.rs` 作为任何层级的入口。

## 14. 配置文件项目

运行参数必须外置到配置文件，不允许把模型名、provider 选择、Telegram bot token 之类的运行信息硬编码进 Rust 源码。

- 默认使用 `config.toml` 作为运行配置。
- 仓库内提供 `config.example.toml` 作为模板。
- 本地真实配置文件 `config.toml` 不应提交到仓库。
- 环境变量可以作为覆盖层，但不能替代配置文件的主体结构。
- 配置文件应覆盖：
  - bot 名称
  - 运行模式
  - 默认 provider
  - provider 的 `base_url` / `model` / `api_key_ref`
  - 存储目录
  - 编辑节流、占位文本等运行参数
- Telegram bot token 单独通过环境变量注入，例如 `TELEGRAM_BOT_TOKEN` 或 `TELOXIDE_TOKEN`，不要写入配置文件。
